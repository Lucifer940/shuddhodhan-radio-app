<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme; all real UI is Jetpack Compose. -->
    <style name="Theme.RadioShuddhodhan" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- System splash screen (androidx.core:core-splashscreen) -->
    <style name="Theme.RadioShuddhodhan.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.RadioShuddhodhan</item>
    </style>
</resources>
